---
layout: layouts/base.njk
textNav:
  aliases: [lab, experiment]
eleventyNavigation:
  key: lab
  parent: bio
---

<article>
  <p>personal projects.</p>

  <ul class="spacer-t">
    {% for experiment in collections.lab -%}
    <li>
      <a class="big-text" href="{{ experiment.url }}" redirect>
        {{ experiment.data.eleventyNavigation.title }}
      </a>
      <br />
      <small>{{ experiment.data.description | safe }}</small>
      {% if experiment.data.instructions != undefined %}
      <br />
      <small><i>{{ experiment.data.instructions | safe }}</i></small>
      {% endif %}
    </li>
    {% endfor %}
  </ul>
</article>
